# 複製成 .env 後填入實際值（.env 已在 .gitignore，勿 commit）

# Neon 連線字串。部署到 Vercel 並接上 Neon 整合後，Vercel 會自動注入這個變數；
# 本機開發時貼上 Neon Console 的連線字串（記得帶 ?sslmode=require）。
DATABASE_URL="postgresql://user:password@ep-xxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require"

# 自簽 JWT 密鑰（HS256）。請換成夠長的隨機字串，例如：openssl rand -base64 48
# Vercel 專案的環境變數也要設定同一個 key。
NUXT_JWT_SECRET="換成長隨機字串"
